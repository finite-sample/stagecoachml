# Requirements for running the latency profiling examples
# These are not required for the core StagecoachML package

# For memory usage tracking in profiler.py
psutil>=5.8.0

# Standard scientific computing dependencies (already in main package)
# numpy>=1.20.0
# pandas>=1.3.0
# scikit-learn>=1.0.0